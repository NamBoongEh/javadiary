1. 2020-12-10 오후 3시 52분 시청

	1. void 타입 메서드에서도 return은 쓸 수 있다.
		
		static void answer(int a){
			if(!(2<=a && a<=9){
				return;
			}
			else {
				System.out.println("야호");
			}
		}
		
		이렇게 쓸 수 있다는 것이다.
		그럼 2~9 사이가 아닌 수는 알아서 제외된다.
		
	2. if(!(2<=a && a<=9){
		해석 : 만약에 a가 2~9 사이의 수가 아닌 경우
		
		if문에서 해당 조건을 제외하고 전체를 말하고싶을때(즉, 여집합을 말하고싶을때) 전체 괄호를 치고 !를 붙이면 된다.