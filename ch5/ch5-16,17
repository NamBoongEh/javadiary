1. 2020-12-09 오후 5시 40분 시청
	새롭게 알게 된 점 :
	1) Scanner로 받는것 외에 직접 입력 값을 커맨드 값을 이용하여 받아보는 방법
		이클립스에 alt + enter 후 location의 주소를 가져온다.
		(나의 경우 C:\Users\Nam\eclipse-workspace\자바의정석studydiary\ch5\ch5-16,17)
		이 뒤에 배열의 값을 넣어주면 알아서 배열이 만들어진다.
		(단, 기본적으로 static void main(String[] args)로 쓰기 때문에 String 형으로 출력된다)
		
		의문점 : 그럼 main을 String[] args에서 int[] args로 바꾸면 숫자만 받을 수 있는지.