1. 2020-12-09 오후 9시 53분 시청 //양식을 그대로 가져와서 시간을 고치지않았다ㅠㅠ
	새롭게 알게 된 점 :
	1) String클래스 = char[](문자배열) + 메서드(기능)
		문자배열에 기능을 추가 한 것이 String 클래스이다.
		
	2) String클래스의 주요 메서드
		
		charAt() = 문자열의 각 문자 중 괄호 안의 번호에 해당하는 인덱스 문자를 반환 (abc에 charAt(0)이면 a)
		length() = 문자열의 길이를 반환 (abc이면 3)
		substring(x, y) = 문자열의 x번 인덱스부터 y-1번***중요*** 인덱스까지 문자를 반환 (abcde이고 substring(1,3)이면 bc)
						(절대 y번까지 들어가는게 아니다!! y-1까지다.)
						+)	substring(x) = 문자열의 x번부터 끝가지 출력한다는 뜻
		equals(aa) = 문자열의 내용이 같은지 확인 (abc.equals(abc)는 true 주로 if문 쓸때 자주 썼었다)
		
		toCharArray() = 문자열을 char[]배열로 바꿔 출력해준다. (아무래도 변수 타입 변환용인것 같다)
		
						ex) String a = "abcde";
							System.out.println(a.toCharArray());
							
							결과 : abcde
	